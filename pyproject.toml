[project]
name = "perk-watch"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "numpy>=2.0",
    "openai>=1.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
perk_watch = ["cards.json"]
